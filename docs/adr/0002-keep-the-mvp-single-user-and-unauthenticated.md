# Keep the MVP single-user and unauthenticated

The MVP has one shared Task List and no user accounts, authentication, or ownership model. This deliberately avoids identity and data-isolation complexity while validating the core Task workflow; deployment must remain local or otherwise trusted until a future decision introduces users and authorization throughout the data model and request flow.
