# Sandcastle Queue Template tool

This project is a Project-controlled Asset. It runs one bounded Queue work unit
through the public `@ai-hero/sandcastle` API. It does not import or require the
Sandcastle Queue Setup package after installation.

Use Node.js 22 and `npm ci`. The host owns GitHub operations. Agent execution
receives only the configured Anthropic-compatible provider credentials.
